import { Settings, Home, Grid3x3 } from 'lucide-react'
import type { ActionSheetAction } from '../../../types/core/action-sheet.types'

export const pageActions = {
  id: '{{PARENT_ID}}',
  actions: [
    {
      id: '{{PARENT_ID}}-overview',
      label: '{{PARENT_NAME}} Overview',
      icon: Home,
      variant: 'default'
    },
    {
      id: '{{PARENT_ID}}-grid',
      label: 'View Grid',
      icon: Grid3x3,
      variant: 'secondary'
    },
    {
      id: '{{PARENT_ID}}-settings',
      label: 'Settings',
      icon: Settings,
      variant: 'secondary'
    }
  ] as ActionSheetAction[]
}