import { EyeOff } from 'lucide-react'
import type { ActionSheetAction } from '../../../types/core/action-sheet.types'

export const pageActions = {
  id: 'chesslayouttest',
  actions: [
    {
      id: 'toggle-chesslayouttest',
      label: 'Toggle ChessLayoutTest',
      icon: EyeOff,
      variant: 'default'
    }
  ] as ActionSheetAction[]
}