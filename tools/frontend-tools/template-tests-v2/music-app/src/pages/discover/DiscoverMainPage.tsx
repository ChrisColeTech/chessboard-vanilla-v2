/*
 * ⚠️  WARNING: GENERATED CODE - DO NOT MODIFY ⚠️
 * 
 * This file is automatically generated by the ParentPageGenerator.
 * Any manual changes will be overwritten when the generator runs.
 * 
 * To make changes, modify the generator in:
 * /tools/frontend-tools/mobile-pages-v2/modules/parent_generator.py
 * 
 * Then re-run the generator to update this file.
 */

import React from "react";
import { TrackList } from "../../components/music/TrackList";
import { MusicVisualizer } from "../../components/player/MusicVisualizer";
import { useMusicLibrary, usePlayer } from "../../stores/musicStore";

interface DiscoverMainPageProps {
  // Add props as needed
}

export const DiscoverMainPage: React.FC<DiscoverMainPageProps> = () => {
  const { topTracks } = useMusicLibrary();
  const player = usePlayer();

  return (
    <div className="uitest-layout-container">
      <div 
        className="w-full h-full border-2 border-border p-2"
        style={{
          display: 'grid',
          gridTemplateColumns: '100px 1fr 100px',
          gridTemplateRows: '80px 1fr 80px',
          gap: '8px',
          minHeight: '100%'
        }}
      >
        {/* Row 1 */}
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Top Left
        </div>
        <div 
          className="flex items-center justify-center border border-border/50 p-4 rounded bg-gradient-to-r from-primary/10 to-secondary/10"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          {player.currentTrack ? (
            <div className="text-center text-muted-foreground">
              <h4 className="text-sm font-medium">Now Playing</h4>
              <p className="text-xs">{player.currentTrack.title}</p>
            </div>
          ) : (
            <div className="text-center text-muted-foreground">
              <h4 className="text-sm font-medium">Discover Top</h4>
              <p className="text-xs">Play music to see what's playing</p>
            </div>
          )}
        </div>
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Top Right
        </div>
        
        {/* Row 2 */}
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Left
        </div>
        <div 
          className="border border-border/50 p-4 rounded overflow-auto"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          <h3 className="text-lg font-semibold mb-3">Top Tracks</h3>
          <TrackList tracks={topTracks.slice(0, 6)} compact />
        </div>
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Right
        </div>
        
        {/* Row 3 */}
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Bottom Left
        </div>
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Bottom
        </div>
        <div 
          className="flex items-center justify-center border border-border/50 p-2 rounded"
          style={{ minHeight: '0', minWidth: '0' }}
        >
          Discover Bottom Right
        </div>
      </div>
    </div>
  );
};